# tower-dominion

Battlefield-first web prototype inspired by the Tower Dominion reference material.

Run locally:

```sh
npm install
npm run dev
```

Then open:

```text
http://127.0.0.1:4173
```

If that port is already occupied, Vite will print the next available local URL.
